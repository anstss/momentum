import React from "react";
import "./settings.scss";

const Settings = () => {
  return (
    <section className="settings col s6 m2">
      <i className="material-icons icon">settings</i>
    </section>
  );
};

export default Settings;
